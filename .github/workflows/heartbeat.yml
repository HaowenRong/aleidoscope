name: Supabase weekly ping
on:
  schedule:
    - cron: '0 0 * * 0' # Sunday at midnight
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Supabase Ping
        run: curl -I "https://${{ secrets.SUPABASE_PROJECT_ID }}.supabase.co/auth/v1/health"
